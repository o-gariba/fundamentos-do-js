// tipo number (primitivo)

const meuNumero = 3;

const num1 = 1;
const num2 = 2;

const opMatematica = num1 + num2;

console.log(opMatematica);

// ponto flutuante

const pontoFlutuante = .6;
const ponto2 = .24;

console.log(pontoFlutuante/ponto2**2);

// Nan = not a number é o resultado quando fizemos operações com numeros e não-numeros

const palavra = "pedro";

console.log(ponto2*palavra);

console.log(0/0); //Nan