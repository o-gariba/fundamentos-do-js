const soma = function(num1, num2) {return num1+num2};

console.log(soma(2,3));

// A função nao tem nome, é anonima

// Diferença principal é que a expressão de função não pode ser usada antes de ser declarada (na const), diferentemente de quando a função é declarada (sem const)

